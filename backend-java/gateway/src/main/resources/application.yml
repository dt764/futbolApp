server:
  port: ${PORT:8083}

spring:
  application:
    name: gateway
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        - id: player-service
          uri: lb://player-service
          predicates:
            - Path=/api/players/**,/api-docs/player-service,/api/ideal-team/**,/api/auth/**,/swagger-ui.html,/swagger-ui/**,/webjars/**,/api-docs
          filters:
            - RewritePath=/api-docs/player-service,/api-docs
        - id: comment-service
          uri: lb://comment-service
          predicates:
            - Path=/api/comments/**,/api-docs/comment-service
          filters:
            - RewritePath=/api-docs/comment-service,/api-docs
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health
