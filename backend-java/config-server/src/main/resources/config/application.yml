# Shared defaults for all services (served by Config Server)

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    registryFetchIntervalSeconds: 5
  instance:
    prefer-ip-address: true

server:
  forward-headers-strategy: framework

springdoc:
  api-docs:
    path: /api-docs
    server-url: https://tactixi.duckdns.org
  swagger-ui:
    path: /swagger-ui.html
    server-url: https://tactixi.duckdns.org

management:
  endpoints:
    web:
      exposure:
        include: health
